# Code Style

- Always use descriptive variable names, never single-letter.
- Prefer inlining over introducing intermediate variables and tiny helper
  methods that are only used once.
- Always use braces for `if`/`while` bodies, even single-line ones.
